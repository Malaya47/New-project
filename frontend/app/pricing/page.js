import PricingPage from "../../components/pricing-page";

export const metadata = {
  title: "Pricing – laundry.li",
  description:
    "Simple, transparent pricing per laundry bag. No complicated rates, no online payment required.",
};

export default function PricingRoute() {
  return <PricingPage />;
}
